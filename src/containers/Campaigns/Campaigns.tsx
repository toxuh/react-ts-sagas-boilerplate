import React from 'react';

import { Section } from '../../components';

const Campaigns: React.FC = () => (
  <Section caption="Here will be caption" title="Campaigns">
    Here will be campaigns
  </Section>
);

export default Campaigns;
